# Day 5: Python Loops

Welcome to Day 5 of my 100 Days of Code challenge! Today I learned about iteration, mastering `for` loops, and utilizing the `range()` function.

## Concepts Learned
* `for` loops with lists
* Using the `range()` function with step sizes
* Accumulating variables (Sums and Max values) manually without built-in functions
* String/List manipulation (`.split()`, `.append()`, `"".join()`)
* Shuffling lists with `random.shuffle()`

## Practice Files include:
* Iterating over lists and slices
* Replicating `sum()` and `max()` logic manually using loops
* Using `range()` to step through numbers

## The Project: PyPassword Generator
A program that asks the user for the desired length of letters, symbols, and numbers, and generates a completely randomized, highly secure password.

## How to run the main project
```bash
python main.py
